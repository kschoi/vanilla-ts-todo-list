{
  "name": "vanilla-ts-todo-list",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --port 3001",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "api": "npx json-server --watch db.json --port 3004"
  },
  "devDependencies": {
    "typescript": "^4.4.4",
    "vite": "^2.7.2"
  },
  "dependencies": {}
}
